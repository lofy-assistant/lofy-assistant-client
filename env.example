# Database Connection (Supabase)
DATABASE_URL=
DIRECT_URL=

# Database Connection (MongoDB Atlas)
MONGODB_URI = 
MONGODB_NAME = 

#Redis Configuration (Upstash)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: cache TTL in seconds (default 300 = 5 minutes)
CACHE_TTL=300

# JWT Secret for session tokens (generate a random string)
JWT_SECRET=

# Phone Encryption Key (32 characters for AES-256)
ENCRYPTION_KEY=

#FASTAPI Connection
FASTAPI_URL=

# Google Cloud Tasks Configuration
GCP_PROJECT_ID=
GCP_LOCATION=us-central1
GCP_SERVICE_URL=

# Stripe Configuration (Required)
# Use pk_test_ with sk_test_ (and live with live). NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY must match
# STRIPE_SECRET_KEY mode so the app picks the correct price catalog (test vs live).
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# App URL (Optional - auto-detected from Vercel or defaults to localhost:3000)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Resend (Email)
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=noreply@yourdomain.com

# Environment
NODE_ENV="development"