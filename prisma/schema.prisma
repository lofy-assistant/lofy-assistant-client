generator client {
  provider = "prisma-client-js"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model users {
  id                      String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  encrypted_phone         String
  hashed_phone            String
  pin                     String?
  email                   String?                   @unique
  name                    String?
  role                    Int                       @default(1)
  metadata                Json?
  created_at              DateTime                  @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at              DateTime                  @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @updatedAt @db.Timestamptz(6)
  ai_persona              String?
  custom_instruction      String?
  calendar_events         calendar_events[]
  feedbacks               feedbacks[]
  integration_credentials integration_credentials[]
  memories                memories[]
  reminders               reminders[]
  subscriptions           subscriptions[]
  sent_friend_invitations friend_invitations[]      @relation("friend_invitations_inviter")
  received_friend_invitations friend_invitations[]  @relation("friend_invitations_invitee")
  friendships_low         friendships[]             @relation("friendships_low_user")
  friendships_high        friendships[]             @relation("friendships_high_user")
  memories_share          memories_share[]
  email_verified              Boolean   @default(false)
  email_verification_token    String?
  email_verification_expires  DateTime? @db.Timestamptz(6)
  default_google_credential_id Int? @unique
  default_google_credential    integration_credentials? @relation("UserDefaultGoogleCredential", fields: [default_google_credential_id], references: [id], onDelete: SetNull, onUpdate: NoAction)
  feature_waitlist              feature_waitlist[]

  @@index([email])
  @@map("users")
}

model friend_invitations {
  id                         String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  inviter_user_id            String       @db.Uuid
  invitee_user_id            String?      @db.Uuid
  invitee_hashed_phone       String
  invitee_encrypted_phone    String
  inviter_display_name       String
  status                     String       @default("pending")
  invite_message_platform_id String?
  expires_at                 DateTime     @default(dbgenerated("(now() + '7 days'::interval)")) @db.Timestamptz(6)
  responded_at               DateTime?    @db.Timestamptz(6)
  accepted_at                DateTime?    @db.Timestamptz(6)
  created_at                 DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?    @default(now()) @db.Timestamptz(6)
  inviter                    users        @relation("friend_invitations_inviter", fields: [inviter_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  invitee                    users?       @relation("friend_invitations_invitee", fields: [invitee_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  friendships                friendships[]

  @@index([invite_message_platform_id], map: "idx_invite_platform_id")
  @@index([invitee_hashed_phone, status], map: "idx_invitee_hash_status")
  @@index([inviter_user_id, status], map: "idx_friend_invitations_inviter_status")
  @@index([invitee_user_id, status], map: "idx_friend_invitations_invitee_status")
  @@map("friend_invitations")
}

model friendships {
  id            BigInt              @id @default(autoincrement()) @db.BigInt
  user_low_id   String              @db.Uuid
  user_high_id  String              @db.Uuid
  invitation_id String?             @db.Uuid
  created_at    DateTime?           @default(now()) @db.Timestamptz(6)
  user_low      users               @relation("friendships_low_user", fields: [user_low_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  user_high     users               @relation("friendships_high_user", fields: [user_high_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  invitation    friend_invitations? @relation(fields: [invitation_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([user_low_id, user_high_id], map: "uq_friendship")
  @@index([user_low_id], map: "idx_friendship_low")
  @@index([user_high_id], map: "idx_friendship_high")
  @@map("friendships")
}

model feedbacks {
  id          Int      @id @default(autoincrement())
  user_id     String   @db.Uuid
  title       String
  description String?
  tag         String
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  updated_at  DateTime @updatedAt @db.Timestamptz(6)
  user        users    @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id])
  @@index([tag])
  @@map("feedbacks")
}

model memories {
  id         Int                    @id @default(autoincrement())
  user_id    String                 @db.Uuid
  title      String?
  content    String
  created_at DateTime               @default(now()) @db.Timestamptz(6)
  updated_at DateTime               @updatedAt @db.Timestamptz(6)
  deleted_at DateTime?              @db.Timestamptz(6)
  embedding  Unsupported("vector")?
  user       users                  @relation(fields: [user_id], references: [id], onDelete: Cascade)
  memories_share memories_share[]

  @@index([user_id])
  @@map("memories")
}

model memories_share {
  id         BigInt   @id @default(autoincrement()) @db.BigInt
  memory_id  Int
  user_id    String   @db.Uuid
  comment    String?
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @updatedAt @db.Timestamptz(6)
  memory     memories @relation(fields: [memory_id], references: [id], onDelete: Cascade)
  user       users    @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@unique([memory_id, user_id], map: "uq_memories_share_memory_user")
  @@index([user_id], map: "idx_memories_share_user_id")
  @@index([memory_id], map: "idx_memories_share_memory_id")
  @@map("memories_share")
}

model calendar_events {
  id              Int        @id @default(autoincrement())
  user_id         String     @db.Uuid
  title           String
  description     String?
  start_time      DateTime   @db.Timestamptz(6)
  end_time        DateTime   @db.Timestamptz(6)
  timezone        String
  is_all_day      Boolean    @default(false)
  created_at      DateTime   @default(now()) @db.Timestamptz(6)
  updated_at      DateTime   @updatedAt @db.Timestamptz(6)
  deleted_at      DateTime?  @db.Timestamptz(6)
  reminder_id     Int?
  recurrence      String?
  next_recurrence DateTime?  @db.Timestamptz(6)
  google_event_id String?
  google_credential_id Int?
  reminders       reminders? @relation(fields: [reminder_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  user            users      @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id])
  @@index([start_time])
  @@index([end_time])
  @@index([next_recurrence])
  @@index([google_credential_id])
  @@map("calendar_events")
}

model reminders {
  id              Int               @id @default(autoincrement())
  user_id         String            @db.Uuid
  target_user_id  String?           @db.Uuid
  message         String
  reminder_time   DateTime          @db.Timestamptz(6)
  status          String
  created_at      DateTime          @default(now()) @db.Timestamptz(6)
  updated_at      DateTime          @updatedAt @db.Timestamptz(6)
  deleted_at      DateTime?         @db.Timestamptz(6)
  recurrence      String?
  next_recurrence DateTime?         @db.Timestamptz(6)
  calendar_events calendar_events[]
  user            users             @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id])
  @@index([target_user_id])
  @@index([reminder_time])
  @@index([status])
  @@index([next_recurrence])
  @@map("reminders")
}

model subscriptions {
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id             String   @db.Uuid
  stripe_customer_id  String?  @unique
  stripe_price_id     String?
  subscription_status String
  current_period_end  DateTime @db.Timestamptz(6)
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  updated_at          DateTime @updatedAt @db.Timestamptz(6)
  user                users    @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id])
  @@index([subscription_status])
  @@map("subscriptions")
}

model integration_credentials {
  id            Int            @id @default(autoincrement())
  user_id       String         @db.Uuid
  provider_name String
  external_id   String?
  display_name  String?
  credentials   Json           @default("{}")
  created_at    DateTime       @default(now()) @db.Timestamptz(6)
  updated_at    DateTime       @updatedAt @db.Timestamptz(6)
  deleted_at    DateTime?      @db.Timestamptz(6)
  user                   users   @relation(fields: [user_id], references: [id], onDelete: Cascade)
  integrations           integrations[]
  user_default_for_google users? @relation("UserDefaultGoogleCredential")

  @@unique([user_id, external_id])
  @@index([user_id])
  @@map("integration_credentials")
}

model integrations {
  id               Int                     @id @default(autoincrement())
  credential_id    Int
  integration_type String
  settings         Json                    @default("{}")
  is_active        Boolean                 @default(true)
  created_at       DateTime                @default(now()) @db.Timestamptz(6)
  updated_at       DateTime                @updatedAt @db.Timestamptz(6)
  deleted_at       DateTime?               @db.Timestamptz(6)
  credential       integration_credentials @relation(fields: [credential_id], references: [id], onDelete: Cascade)

  @@index([credential_id])
  @@map("integrations")
}

model upvotes {
  id             Int      @id @default(autoincrement())
  user_id        String   @db.Uuid
  integration_id String
  created_at     DateTime @default(now()) @db.Timestamptz(6)

  @@unique([user_id, integration_id])
  @@index([user_id])
  @@map("upvotes")
}

model upvote_counts {
  id             Int    @id @default(autoincrement())
  integration_id String @unique
  count          Int    @default(0)

  @@map("upvote_counts")
}

model feature_waitlist {
  id         Int      @id @default(autoincrement())
  user_id    String   @db.Uuid
  feature    String
  created_at DateTime @default(now()) @db.Timestamptz(6)
  user       users    @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@unique([user_id, feature], map: "uq_feature_waitlist_user_feature")
  @@index([feature], map: "idx_feature_waitlist_feature")
  @@index([user_id], map: "idx_feature_waitlist_user_id")
  @@map("feature_waitlist")
}

model lofy_team {
  id            Int       @id @default(autoincrement())
  name          String
  display_name  String?
  email         String    @unique
  role_id       Int
  is_active     Boolean   @default(true)
  password_hash String
  last_login_at DateTime? @db.Timestamptz(6)
  phone         String?   @db.VarChar(20)
  timezone      String?   @default("Asia/Kuala_Lumpur")
  department    String?
  position      String?
  created_at    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime  @updatedAt @db.Timestamptz(6)
  deleted_at    DateTime? @db.Timestamptz(6)
  lofy_id       String    @unique @db.VarChar

  @@map("lofy_team")
}
